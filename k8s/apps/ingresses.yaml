# Ingress resources for all domains
---
# Personal Website - msdqn.dev
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: personal-website
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Rate limiting (static site - higher limits)
    nginx.ingress.kubernetes.io/limit-rps: "50"
    nginx.ingress.kubernetes.io/limit-connections: "30"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "10"
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - msdqn.dev
    - www.msdqn.dev
    secretName: personal-website-tls
  rules:
  - host: msdqn.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: personal-website
            port:
              number: 4321
  - host: www.msdqn.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: personal-website
            port:
              number: 4321
---
# RKM Backend - api.rajawalikaryamulya.co.id
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rkm-backend
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    # Rate limiting (API - moderate limits)
    nginx.ingress.kubernetes.io/limit-rps: "10"
    nginx.ingress.kubernetes.io/limit-rpm: "300"
    nginx.ingress.kubernetes.io/limit-connections: "20"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "5"
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - api.rajawalikaryamulya.co.id
    secretName: rkm-backend-tls
  rules:
  - host: api.rajawalikaryamulya.co.id
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rkm-backend
            port:
              number: 3300
---
# RKM Frontend - rajawalikaryamulya.co.id (static served by nginx module)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rkm-frontend
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    # Rate limiting (static site - higher limits)
    nginx.ingress.kubernetes.io/limit-rps: "50"
    nginx.ingress.kubernetes.io/limit-connections: "30"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "10"
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - rajawalikaryamulya.co.id
    - www.rajawalikaryamulya.co.id
    secretName: rkm-frontend-tls
  rules:
  - host: rajawalikaryamulya.co.id
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rkm-frontend
            port:
              number: 8080
  - host: www.rajawalikaryamulya.co.id
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rkm-frontend
            port:
              number: 8080
---
# RKM Admin Frontend - cms.rajawalikaryamulya.co.id
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rkm-admin-frontend
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Rate limiting (admin panel - moderate limits)
    nginx.ingress.kubernetes.io/limit-rps: "20"
    nginx.ingress.kubernetes.io/limit-rpm: "600"
    nginx.ingress.kubernetes.io/limit-connections: "15"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "5"
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - cms.rajawalikaryamulya.co.id
    secretName: rkm-admin-frontend-tls
  rules:
  - host: cms.rajawalikaryamulya.co.id
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rkm-admin-frontend
            port:
              number: 3100
---
# Roasting Startup - roast.kilat.app
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: roasting-startup
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Rate limiting (AI service - stricter limits)
    nginx.ingress.kubernetes.io/limit-rps: "5"
    nginx.ingress.kubernetes.io/limit-rpm: "120"
    nginx.ingress.kubernetes.io/limit-connections: "10"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "3"
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - roast.kilat.app
    secretName: roasting-startup-tls
  rules:
  - host: roast.kilat.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: roasting-startup
            port:
              number: 7676
---
# Kilat App Frontend - kilat.app
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kilat-frontend
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Rate limiting (static frontend - higher limits)
    nginx.ingress.kubernetes.io/limit-rps: "50"
    nginx.ingress.kubernetes.io/limit-connections: "30"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "10"
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - kilat.app
    secretName: kilat-frontend-tls
  rules:
  - host: kilat.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kilat-frontend
            port:
              number: 8080
---
# Kilat App API - api.kilat.app
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kilat-api
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    # Rate limiting (API - moderate limits)
    nginx.ingress.kubernetes.io/limit-rps: "10"
    nginx.ingress.kubernetes.io/limit-rpm: "300"
    nginx.ingress.kubernetes.io/limit-connections: "20"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "5"
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - api.kilat.app
    secretName: kilat-api-tls
  rules:
  - host: api.kilat.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kilat-server
            port:
              number: 8082
---
# Verychic Frontend - verychic.msdqn.dev
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: verychic-frontend
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Rate limiting (static site - higher limits)
    nginx.ingress.kubernetes.io/limit-rps: "50"
    nginx.ingress.kubernetes.io/limit-connections: "30"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "10"
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - verychic.msdqn.dev
    secretName: verychic-frontend-tls
  rules:
  - host: verychic.msdqn.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: verychic-frontend
            port:
              number: 8443
---
# MinIO S3 - s3.msdqn.dev and storage.kilat.app
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio-s3
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1g"
    # Rate limiting (storage uploads - stricter limits)
    nginx.ingress.kubernetes.io/limit-rps: "20"
    nginx.ingress.kubernetes.io/limit-connections: "10"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "3"
    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - s3.msdqn.dev
    - storage.kilat.app
    secretName: minio-s3-tls
  rules:
  - host: s3.msdqn.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: minio
            port:
              number: 9000
  - host: storage.kilat.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: minio
            port:
              number: 9000
